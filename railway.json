{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd application && python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn application.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"
  }
}
